Hi <%= @user.name %>

<%= @current_user.name %> (<%= @current_user.email %>) has requested to join your carpool.

<%= @current_user.name %>
lives at:
<%= @current_user.address %>
<%= @current_user.locality.name %>

More information of the user: <%= user_show_url(id: @current_user.id) %>

If you approve, <%= @current_user.name %> will be added to your carpool, and a confirmation
email will be sent to <%= @current_user.name %>.

What would you like to do?

Approve: <%= approve_reject_handler_url(token: @digest, user: @current_user.id, cabpool: @cabpool, approver: @user.id, approve: true) %>

Reject: <%= approve_reject_handler_url(token: @digest, user: @current_user.id, cabpool: @cabpool, approver: @user.id, approve: false) %>

Thanks!

Danglay
