Hi Admin!

<%= @user.name %> (<%= @user.email %>) has requested for a cabpool having ID: <%= displayable @cabpool %>.

<%= @user.name %> lives at:
<%= @user.address %>

If you approve, <%= @user.name %> will be added to your carpool, and a confirmation email will be sent to <%= @user.name %>.

What would you like to do?

<%= approve_reject_handler_url(token: @digest, user: @user.id, cabpool: @cabpool, approve: true) %>

<%= approve_reject_handler_url(token: @digest, user: @user.id, cabpool: @cabpool, approve: false) %>

Thanks!
Danglay
